# GAS開発テンプレート（非エンジニア向け）

## 概要
このリポジトリは、非エンジニアがClaude Codeを使ってGoogle Apps Script（GAS）を開発するためのひな型です。
開発の知識は不要です。やりたいことを日本語でClaude Codeに伝えるだけで、コードの作成からGoogleへの反映まですべてClaude Codeが行います。

---

## はじめに：セットアップ手順

### ステップ1：Node.js をインストールする

Node.js は、clasp（GASをローカルから操作するツール）を動かすために必要です。

1. ブラウザで https://nodejs.org を開く
2. 「LTS」と書かれた緑のボタンをクリックしてダウンロード
3. ダウンロードしたファイルをダブルクリックして開く
4. インストーラーが表示されるので「Next」を押し続けて最後に「Install」→「Finish」
5. すべてデフォルト（初期設定）のままでOKです

### ステップ2：Git をインストールする

Git は、コードの変更履歴を記録するツールです。「昨日の状態に戻したい」ができるようになります。

1. ブラウザで https://git-scm.com を開く
2. 「Download for Windows」をクリックしてダウンロード
3. ダウンロードしたファイルをダブルクリックして開く
4. インストーラーが表示されるので「Next」を押し続けて最後に「Install」→「Finish」
5. すべてデフォルト（初期設定）のままでOKです

### ステップ3：Apps Script API を有効にする

この設定をしないと、clasp が Google にアクセスできません。

1. ブラウザで https://script.google.com を開く（Googleアカウントでログインしてください）
2. 左側メニューの「設定」（歯車アイコン）をクリック
3. 「Google Apps Script API」を「オン」に切り替える

### ステップ4：Claude Codeに残りをお願いする

ここからはClaude Codeが自動で行います。
Claudeデスクトップアプリの「コード」タブを開き、以下のように伝えてください：

> 「GASの開発環境をセットアップして」

Claude Codeが以下を自動で実行します：
- clasp のインストール（`npm install @google/clasp -g`）
- Googleアカウントとの連携（`clasp login`）
  → このときブラウザが開くので、Googleアカウントでログインしてください
  → 「許可」を押してください
- プロジェクトフォルダの初期化
- 必要なファイルの作成

### セットアップ完了の確認

セットアップがうまくいったかどうかは、Claude Codeに聞いてください：

> 「セットアップできてるか確認して」

---

## 日常の開発の流れ

セットアップは最初の1回だけです。以降は以下の流れで開発します。

### あなたがやること
1. Claudeデスクトップアプリの「コード」タブを開く
2. やりたいことを日本語で伝える

### 伝え方の例
- 「シートAのA列〜C列のデータを集計して、シートBに書き出す機能を作って」
- 「毎朝9時に自動でメールを送る機能を追加して」
- 「エラーが出たので直して」（エラーメッセージを貼り付けてください）
- 「昨日の状態に戻して」

### Claude Codeがやること
1. コードを書く（または修正する）
2. Googleに反映する（`clasp push`）
3. 必要に応じてデプロイする

### あなたがやること（続き）
3. スプレッドシートを開いて動作確認する
4. 問題があればClaude Codeに伝える

---

## プロジェクト構成

```
gas-template-for-beginners/
├── CLAUDE.md          # このファイル（Claude Codeへの説明書）
├── .clasp.json        # clasp設定（自動生成される）
├── .claspignore       # Googleに送らないファイルの指定
├── appsscript.json    # GASの設定
└── src/               # コード置き場
    └── main.gs        # メインの処理
```

---

## 環境
- ランタイム: Google Apps Script（V8）
- デプロイツール: clasp
- ファイル形式: `.gs`（JavaScript）

---

## GASの制限事項（知っておくと便利）
- 1回の実行は最大6分（長い処理は分割が必要）
- メール送信は1日100通まで
- async/awaitは使えない（同期処理で書く）

---

## Claude Codeへの指示（Claude Code向け）
- ユーザーは開発経験のない非エンジニアである
- 説明はすべて日本語で、専門用語を避けてわかりやすく行うこと
- コードを書いたら `clasp push` でGoogleに反映すること
- エラーが出たら原因と対処法をわかりやすく説明すること
- 操作を求める場合は具体的な手順を示すこと（「ブラウザで○○を開いて、△△をクリックしてください」のように）
- 変更を加えたらこのCLAUDE.mdの「スプレッドシート情報」も更新すること

---

## スプレッドシート情報
<!-- プロジェクト開始時にClaude Codeが記載します -->
<!-- 例: -->
<!-- - スプレッドシートURL: https://docs.google.com/spreadsheets/d/xxxxx -->
<!-- - シートA: A列=日付, B列=商品名, C列=金額 -->
<!-- - シートB: 集計結果の出力先 -->
